<configure xmlns="http://namespaces.zope.org/zope">

  <!-- Intentionally empty since 2.0: the IPubFailure handler is registered
       programmatically by ZopeIntegration.setup_once(). This file is kept
       so the z3c.autoinclude entry point keeps importing the package
       (which triggers the env-var bootstrap in __init__). -->

</configure>
